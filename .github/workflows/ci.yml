name: Deprecated Java CI (disabled)

# This workflow has been superseded by `.github/workflows/tests.yml`.
# Kept for history; it will only run when manually triggered.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Echo disabled
        run: echo "This workflow is deprecated. Use .github/workflows/tests.yml instead."
